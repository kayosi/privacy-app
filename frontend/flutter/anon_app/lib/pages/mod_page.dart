import 'package:flutter/material.dart';

class ModPage extends StatelessWidget {
  const ModPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('📝 This is mod page', style: TextStyle(color: Colors.white)),
    );
  }
}
